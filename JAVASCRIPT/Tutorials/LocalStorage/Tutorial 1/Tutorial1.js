//try to keep array in local storage
//addTodo(){
//    addToUI()
//    addToStorage() make an array in localstorage
//  }
// let data = localStorage('todos') should be converted into js object
// set eventtype for all document with domcontentloaded when u visit back page it will start automaticly
// in function use forEach
// iterate data object > print todo title with model
// model = html block bv.: <p></p>
// forEacht() loop{
// innerHTMK += <p> ${todo.title} </p> }


//opgesomt:
// documentloaded
// use forEach
// iterate data object >print todo title with model
// .forEach(){
// innerHTML + = <p> ${todo.title} </p> }
// }

//window add